import styled from "styled-components";

export const SectionSuperior = styled.section`

  display: flex;
  align-items: center;
  justify-content: space-around;
  flex-wrap: wrap;
  margin-top: 600px;
  padding-bottom: 20px;
  width: 90vw;
  border-bottom: 1px solid var(--grey-2);

  h1 {
    font-size: 18px;
    font-weight: 700;
    line-height: 28px;

    color: var(--grey-0);

  }
  p {
    font-size: 12px;
    font-weight: 400;
    line-height: 22px;

    color: var(--grey-1);
  }

`